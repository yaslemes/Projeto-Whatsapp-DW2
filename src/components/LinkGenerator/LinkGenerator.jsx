import React from "react";

export default function LinkGenerator() {
  return (
    <div
      style={{
        minHeight: "300px", 
        background: "#fff",
        borderRadius: "12px",
        boxShadow: "0px 2px 6px rgba(0,0,0,0.1)",
        padding: "20px",
      }}
    >
      {/* Codigo isa, oii:) */}
    </div>
  );
}
